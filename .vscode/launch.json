{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Michi's Adventure (Desktop)",
            "request": "launch",
            "mainClass": "desktop.DesktopLauncher",
            "projectName": "desktop",
            "cwd": "${workspaceFolder}"
        }
    ]
}
