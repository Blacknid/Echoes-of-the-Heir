// Michi's Adventure — libGDX build (desktop branch).
//
// Modules:
//   core    — engine-agnostic game logic + libGDX rendering.
//   desktop — desktop launcher (LWJGL3 backend) + jpackage/installer packaging.
rootProject.name = 'michis-adventure'

include 'core'
include 'desktop'
