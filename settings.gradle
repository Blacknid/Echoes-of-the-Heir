// Michi's Adventure — libGDX build (android branch).
//
// Modules:
//   core    — engine-agnostic game logic + libGDX rendering.
//   android — Android launcher (gdx-backend-android).
rootProject.name = 'michis-adventure'

include 'core'
include 'android'
